# Render Blueprint - https://render.com/docs/blueprint-spec
# Deploy: Connect GitHub repo in Render Dashboard

services:
  - type: web
    name: feishu-approval-bot
    runtime: python
    region: frankfurt  # Closest to Stockholm
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:APP --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
      # Secrets - set these in Render Dashboard
      - key: FEISHU_APP_ID
        sync: false
      - key: FEISHU_APP_SECRET
        sync: false
      - key: FEISHU_VERIFICATION_TOKEN
        sync: false
      - key: FEISHU_SIGNING_SECRET
        sync: false
      # Dropbox (https://www.dropbox.com/developers/apps)
      - key: DROPBOX_REFRESH_TOKEN
        sync: false
      - key: DROPBOX_APP_KEY
        sync: false
      - key: DROPBOX_APP_SECRET
        sync: false
      # Resend Email (https://resend.com)
      - key: RESEND_API_KEY
        sync: false
      - key: RESEND_FROM_EMAIL
        sync: false
      # Target Fortnox emails per approval type
      - key: EMAIL_EXPENSE
        sync: false
      - key: EMAIL_PAYMENT_SHIC
        sync: false
    healthCheckPath: /health
